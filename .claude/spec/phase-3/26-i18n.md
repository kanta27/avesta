# 26 — Regional language (i18n)

**Phase:** 3 · **Depends on:** A1 (component architecture) · **Status:** To build

## Purpose
Regional-language support, architected now to avoid a later rewrite.

## Build
- Next.js i18n routing.
- **Externalize copy now** (even if shipped later) so components are locale-aware from the start.

## Tech notes
- Keep all user-facing strings in message catalogs, not inline JSX, from the earliest build — this is the cheap-now/expensive-later item flagged in the spec.
- Plan locale-aware metadata + structured data for SEO per language.

## Acceptance
- A language toggle switches UI copy.
- Component architecture is already locale-aware (no hardcoded user-facing strings).
